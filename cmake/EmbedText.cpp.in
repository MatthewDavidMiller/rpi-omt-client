// Generated by cmake/EmbedText.cmake from the repository's legal texts.
// Do not edit: rebuild instead.

#include "legal_texts.hpp"

namespace omt::deployer {
namespace {

// `unsigned char` because the texts are UTF-8. They happen to be ASCII today,
// but a byte above 0x7F does not narrow into a plain `char` where `char` is
// signed, and a notice for a new component must not break the build.
@arrays@
std::string_view text_of(const unsigned char* bytes, std::size_t size) noexcept {
    return {reinterpret_cast<const char*>(bytes), size};
}

}  // namespace

std::span<const LegalDocument> legal_documents() noexcept {
    // Function-local so the table is built on first use rather than depending
    // on static initialization order.
    static const LegalDocument documents[] = {
@table@    };
    return documents;
}

}  // namespace omt::deployer
