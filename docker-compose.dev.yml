services:
  omt-client:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    container_name: omt-client-dev
    ports:
      - "5000:5000"
    volumes:
      - omt-config-dev:/etc/omt

volumes:
  omt-config-dev:
